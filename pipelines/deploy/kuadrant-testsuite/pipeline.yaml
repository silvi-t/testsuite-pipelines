apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: deploy-kuadrant
spec:
  params:
  - description: API URL of the Openshift cluster
    name: kube-api
    type: string
  - description: Secret name with cluster credentials
    name: cluster-credentials
    type: string
    default: openshift-pipelines-credentials
  - description: Kuadrant image url
    name: index-image
    type: string
    default: quay.io/kuadrant/kuadrant-operator-catalog:v1.3.1
  - description: Helm chart version to install. If set, INDEX-IMAGE IS IGNORED.
    name: chart-version
    type: string
    default: ""
  - description: Kuadrant image channel. Can be 'preview' for nightlies and 'stable' for releases
    name: channel
    type: string
    default: stable
  - description: Kuadrant operator name. 'kuadrant-operator' or 'rhcl-operator'
    name: operator-name
    type: string
    default: kuadrant-operator
  - description: Istio provider (ossm3, ocp); 'ossm3' (recommended for all Openshift versions), 'ocp' for installing Istio managed by Gateway API on OCP v4.19+ (not recommended - mTLS is not supported)
    name: istio-provider
    type: string
    default: ossm3
  - description: Image for the standalone Kuadrant extension (e.g. quay.io/kuadrant/internal-extensions:latest). Extensions are only supported on Kuadrant v1.6+ / RHCL v1.5+ (leave empty to skip deploying extensions)
    name: extensions-image
    type: string
    default: ""
  - description: Additional flags for helm install command, example '--set=kuadrant.installPlanApproval=Manual --set=kuadrant.startingCSV=kuadrant-operator.v1.2.0'
    name: additional-helm-flags
    type: string
    default: ""
  - description: Additional flags for tools helm, example '--set=tools.keycloak.keycloakProvider=deployment --set=tools.valkey.enable=false'"
    name: additional-helm-tools-flags
    type: string
    default: ""
  - description: Additional flags for the extensions helm install command, example '--set=kuadrant.extensionsNamespace=kuadrant-extensions'
    name: extensions-additional-helm-flags
    type: string
    default: ""
  tasks:
  - name: check-image-existence
    params:
      - name: index-image
        value: $(params.index-image)
    taskRef:
      kind: Task
      name: check-image-existence
  - name: kubectl-login
    params:
    - name: kube-api
      value: $(params.kube-api)
    - name: testsuite-image
      value: quay.io/kuadrant/testsuite-pipelines-tools:latest
    - name: cluster-credentials
      value: $(params.cluster-credentials)
    taskRef:
      kind: Task
      name: kubectl-login
    workspaces:
    - name: shared-workspace
      workspace: shared-workspace
  - name: helm-deploy
    retries: 3
    runAfter:
    - kubectl-login
    - check-image-existence
    taskRef:
      kind: Task
      name: helm-deploy
    params:
      - name: index-image
        value: $(params.index-image)
      - name: chart-version
        value: $(params.chart-version)
      - name: channel
        value: $(params.channel)
      - name: istio-provider
        value: $(params.istio-provider)
      - name: operator-name
        value: $(params.operator-name)
      - name: kubeconfig-path
        value: $(tasks.kubectl-login.results.kubeconfig-path)
      - name: additional-helm-flags
        value: $(params.additional-helm-flags)
      - name: additional-helm-tools-flags
        value: $(params.additional-helm-tools-flags)
      - name: install-tools
        value: "true"
      - name: install-kuadrant
        value: "true"
      - name: additional-manifests-secret
        value: values-additional-manifests
    workspaces:
    - name: shared-workspace
      workspace: shared-workspace
  - name: deploy-extensions
    retries: 3
    runAfter:
    - helm-deploy
    when:
    - input: $(params.extensions-image)
      operator: notin
      values: [""]
    taskRef:
      kind: Task
      name: deploy-extensions
    params:
      - name: kubeconfig-path
        value: $(tasks.kubectl-login.results.kubeconfig-path)
      - name: extensions-image
        value: $(params.extensions-image)
      - name: extension-crd-cm
        value: extension-crd
      - name: additional-helm-flags
        value: $(params.extensions-additional-helm-flags)
    workspaces:
    - name: shared-workspace
      workspace: shared-workspace
  workspaces:
  - name: shared-workspace
